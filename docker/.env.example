# Local Postgres superuser (migrations + writes). Reads use cinema_ro, see infra/postgres/roles.sql.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=cinema
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379
