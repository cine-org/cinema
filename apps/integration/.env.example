DATABASE_URL=postgresql://cinema:cinema@localhost:5432/cinema

# Must be identical to the API value. Base64-encoded 32-byte AES key.
AUTH_OUTBOX_ENCRYPTION_KEY=
WEB_ORIGIN=http://localhost:4000

SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM=Cinema <noreply@cinema.local>
OUTBOX_POLL_INTERVAL_MS=5000
