// Kept as a compatibility entry point for code that imported the old internal file.
export {
  DatabaseClient,
  createDatabaseClient,
  type CreateDatabaseClientOptions,
  type DatabaseClientOptions,
} from './database.client';
